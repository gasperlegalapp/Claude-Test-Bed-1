{
  "name": "firm",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "proforma": "vite --config proforma/vite.config.ts",
    "proforma:build": "tsc && vite build --config proforma/vite.config.ts",
    "proforma:preview": "vite preview --config proforma/vite.config.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.5"
  }
}
